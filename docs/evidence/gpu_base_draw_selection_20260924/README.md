# GPU base draw-selection diagnosis (2026-09-24)

The GPU candidate is not ready for performance tuning. Its native processing state can report an LOD0 chunk as `visible` while the renderer does not select any LOD0 terrain draw. The base-coverage cut selects an active LOD3 root before considering its descendants (`wt_terrain_gpu_base_coverage.gd::_cover`). A complete LOD3 root therefore hides all finer active chunks within it.

To distinguish processing from presentation, the renderer now reports `selected_terrain_lod_counts` alongside `active_terrain_lod_counts`. The bounded `base_coverage_edit_probe` prints both per sample. This instrumentation changes no draw or collision behavior.

Reproduction: `python tools/probe_gpu_base_startup.py --godot 'C:\Program Files (x86)\Steam\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe' --seconds 12 --memory-gib 3 --mode base_coverage_edit_probe` on game commit `62daa32` plus this diagnostic. Probe exited 0 in 9.82 s; peak RSS was 741 MiB. At frame 20 / 499 ms after edit, 10 LOD0 and 13 LOD3 chunks were active, but the selected terrain counts were only 13 LOD3. At frame 44 / 1235 ms, 10 LOD0 chunks remained active, but the selected terrain counts were only 20 LOD3. The local native processing state called LOD0 `visible` at both times. The output is in `.godot/world_transvoxel_captures/base_coverage_probe/godot.log`.

This is a presentation correctness failure. The draw cut must be rebuilt as an atomic, complete, balanced mixed-LOD cover over the loaded base. For a selected child, the corresponding parent regular brick must be suppressed in the same publication; all uncovered parent bricks and required transition geometry must remain drawn. Failed or incomplete child groups must leave their old cover intact. A test must assert actual selected draw counts and geometry/revision at the edited point; native `visible` state alone is not an acceptance criterion. Only after that gate passes should edit latency, streaming throughput, or wattage be optimized.

The bounded probe now exits nonzero when active LOD0 never becomes selected for drawing. On the current candidate it fails as intended: Godot completed and captured an image in 10.59 s (718 MiB peak RSS), but the final sample had six active LOD0 chunks and zero selected LOD0 chunks. This gate rejects the earlier false-positive result while retaining the capture for inspection.

The next implementation must connect the existing partial-parent input format to actual runtime capture and publication. The native sample-job caller currently uses the full-mask overload, so all live parent captures have mask `0xff` and no internal transition inventory. Selecting children without first preparing a masked parent would cause overlap or cracks. The safe unit of publication is the masked parent candidate, its required direct children and internal transitions, with the previous complete cover retained until the whole unit validates.

The renderer cut walker now prefers a fully validated finer subtree over an available coarse parent. It also accepts a previously captured partial parent only when every hidden brick has a selected child; a missing child rejects that root cut, retaining the base atlas. The focused Godot cut smoke passes these cover cases. This is an incomplete checkpoint: the 10.9 s bounded gameplay probe still fails its draw-selection gate, ending with seven active LOD0 chunks, zero selected LOD0 chunks, and nineteen selected LOD3 chunks. Live native parent captures still carry full masks, so this renderer change cannot yet refine an individual interaction region.

One temporary read-only trace sampled the native completed visual topology and GPU processing stages at the edit point. At frame 44 / 1915 ms, the completed plan contained 32 LOD0, 84 LOD1, 13 LOD2, and 416 LOD3 leaves. The GPU had eight active LOD0 and sixteen active LOD3 chunks, but no active LOD1 or LOD2; one LOD1 was extracting, seventeen LOD1 were waiting for a cohort, and no LOD2 had entered GPU processing. The local LOD3 and LOD0 records were both revision 1 with regular visibility mask `0xff`; the draw cut still selected only LOD3. The process hit the 15 s wall-time cap while writing the visual capture (804 MiB peak RSS), but the `WT_GPU_BASE_EDIT_PROBE` trace was emitted in `.godot/world_transvoxel_captures/base_coverage_probe/godot.log`. The temporary per-stage/topology sampler was removed after extracting this evidence.

A native scheduling experiment boosted all intermediate leaves around the focus. The existing foreground projection test showed it would elevate 56 LOD1 leaves for a 3×3×3 LOD0 focus. That is a capture flood, not the bounded local cut required here, so the experiment was reverted and the native debug build restored. The unchanged production LOD streaming native test passes. The next code must track only the necessary masked parent chain and its direct children as a bounded, generation/revision-safe publication cohort; increasing whole-plan priority is rejected by this evidence.
